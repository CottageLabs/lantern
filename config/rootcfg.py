# absolute paths, or relative paths from root directory, to the desired config files
CONFIG_FILES = [
    "portentious/portality/config/webapp.py"
]

# absolute paths, or relative paths from root directory, to the template directories
TEMPLATE_PATHS = [
    "portentious/portality/templates"
]